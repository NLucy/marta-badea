# Marta Ileana Badea Portfolio

Vercel static portfolio with a serverless interview assistant.

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

Set `OPENAI_API_KEY` in `.env.local` locally and in Vercel for the chat feature.
